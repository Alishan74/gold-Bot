pandas>=2.0
numpy>=1.24
pyarrow>=14.0
requests>=2.31
fastapi>=0.110
uvicorn>=0.29
scikit-learn>=1.4
joblib>=1.3
scipy>=1.11

# Optional - per-signal SHAP factor attribution (ml_system/live_signal.py
# degrades to "no factor breakdown" if this isn't installed, never
# crashes). Uncomment to enable:
# shap>=0.44
